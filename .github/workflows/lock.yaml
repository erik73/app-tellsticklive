---
name: Lock

# yamllint disable-line rule:truthy
on:
  schedule:
    - cron: "0 9 * * *"
  workflow_dispatch:

permissions:
  issues: write
  pull-requests: write

jobs:
  workflows:
    uses: erik73/workflows/.github/workflows/lock.yaml@6e107bbfcc7007ffc686df8999a3dde510f8c251 # v2.0.2
